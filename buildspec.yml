version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "Installing dependencies..."
      - npm ci
  build:
    commands:
      - echo "Build step (not needed for this simple app)"
artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: '.'   # include everything so appspec + scripts go into artifact

